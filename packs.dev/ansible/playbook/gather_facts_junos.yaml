---
- hosts: junos
  connection: network_cli
  gather_facts: no
  become: yes

  tasks:
    - name: show version
      junos_command:
        commands: show version
      register: result
      when: ansible_network_os == "junos"
    - debug: 
        msg: "{{ result }}"

